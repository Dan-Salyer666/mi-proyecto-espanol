# Los Paisajes — Image Correction Checklist

Fixes needed on the two map images so they agree with each other and with the
quiz vocabulary. Files:

- English map → `assets/vocabulario/landscapes in english.webp`
- Spanish map → `assets/vocabulario/paisajes en espanol.webp`

---

## English map — `landscapes in english.webp`

- [ ] **"Ejard" → "Fjord"** — garbled label. Sits where the Spanish map correctly
      reads *el fiordo*.

- [ ] **Add "Ice Floe"** — the Spanish map labels *el témpano de hielo* next to the
      iceberg, but the English map only labels "Iceberg." Add an "Ice Floe" label on
      the flatter ice piece so the two sides match.
      *(Alternative: if you'd rather not have the extra term, delete* el témpano de hielo
      *from the Spanish map instead — but right now the two maps disagree.)*

- [ ] **Add "Cave"** — see the cove/cave note below. English currently has "Cove"
      but no "Cave"; the cave feature (Spanish *la cueva*) has no English label.

---

## Spanish map — `paisajes en espanol.webp`

- [ ] **Remove the duplicate "La selva tropical"** — it appears twice (once at the
      central rainforest, once lower-left near the waterfall). The English map labels
      "Rain Forest" only once. Delete the stray lower-left one.

- [ ] **Add the cove label "la cala / la ensenada"** — see the cove/cave note below.
      Spanish currently has only *la cueva* (cave); the cove feature has no Spanish label.

- [ ] **Standardize article capitalization** — labels are mixed: some capitalize the
      article (*El lago*, *La montaña*, *La bahía*) and others lowercase it (*el valle*,
      *el cerro*, *el desierto*, *la meseta*, *el estrecho*, *el canal*, *la playa*,
      *el fiordo*). Pick one and apply it throughout. Suggestion: lowercase article on
      every label, since these are vocab tags rather than sentence-initial headings.

---

## The cove / cave situation (read before fixing either map)

Right now the two maps **disagree on one spot**: the English map calls it **"Cove,"**
the Spanish map calls it **"La cueva"** (= cave). A cove (a small coastal inlet) and a
cave (an opening in rock) are different things, so one of them is wrong as-is.

Plan (per your decision to keep **both** in the quiz):

1. Decide what the artwork at that spot actually depicts — a cave or a cove.
2. Label **that** feature consistently on both maps:
   - cave → English **"Cave"** / Spanish ***la cueva***
   - cove → English **"Cove"** / Spanish ***la cala* / *la ensenada***
3. Add a **second** label elsewhere for the other feature, so both vocabulary words
   appear on the maps. (If there's no second feature in the art to point at, the quiz
   will still cover both words regardless of what the image shows.)

Quiz already reflects this:

- **the cave** → accepts `la cueva`
- **the cove** → accepts `la cala` **or** `la ensenada` (either is correct)

---

## Already correct on both maps (no action)

These match across the two images and are now in the quiz pool — no change needed:

- Forest / *el bosque*
- Glacier / *el glaciar*
- Channel / *el canal*

---

## Summary

| # | Image            | Fix                                              | Type        |
|---|------------------|--------------------------------------------------|-------------|
| 1 | English          | "Ejard" → "Fjord"                                | typo        |
| 2 | English          | Add "Ice Floe" (matches *el témpano de hielo*)   | missing     |
| 3 | English          | Add "Cave" + resolve Cove/Cave (see note)        | mismatch    |
| 4 | Spanish          | Remove duplicate "La selva tropical"             | duplicate   |
| 5 | Spanish          | Add cove label "la cala / la ensenada"           | missing     |
| 6 | Spanish          | Standardize article capitalization (El vs el)    | cosmetic    |

Items 1–5 are genuine mismatches/errors; item 6 is cosmetic.
